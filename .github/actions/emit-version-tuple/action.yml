name: Emit version-tuple envelope (minimal diagnostic)
description: Diagnostic minimal composite action.
inputs:
  project:
    description: "test"
    required: true
  ran:
    description: "test"
    required: true
  field:
    description: "test"
    required: true
  value:
    description: "test"
    required: true
  depends_on:
    description: "test"
    required: true
  pq_signing_key:
    description: "test"
    required: true
  pq_kid:
    description: "test"
    required: true
  ed25519_signing_key:
    description: "test"
    required: false
    default: ""
  ed25519_kid:
    description: "test"
    required: false
    default: ""
  release_tag:
    description: "test"
    required: true
runs:
  using: composite
  steps:
    - name: Echo inputs
      shell: bash
      run: |
        echo "project=${{ inputs.project }}"
        echo "field=${{ inputs.field }}"
        echo "value=${{ inputs.value }}"
        echo "(diagnostic minimal action - emit step removed)"
